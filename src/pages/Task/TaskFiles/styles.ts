import { makeStyles } from '@mui/styles';

export const useTaskFileStyles = makeStyles((theme) => ({
    files: {
        position: 'relative',
        margin: theme.spacing(3, 0),
    },
}));
