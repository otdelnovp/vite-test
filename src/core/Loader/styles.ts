import { makeStyles } from '@mui/styles';

export const useLoaderStyles = makeStyles({
    loader: {
        position: 'relative',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
    },
    customLoader: {},
});
